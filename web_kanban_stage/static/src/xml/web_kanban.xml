<template>

    <t t-name="KanbanView.SelectStage">
        <div class="o_column_add_stage">
            <div class="o_stage_header">
                <span class="o_stage_header_column"><i class="fa fa-chevron-right"/></span>
                <span class="o_stage_header_title">Add Old Stages</span>
            </div>
            <div class="o_kanban_add_stage">
                <select class="o_stage_select"></select>
                <button class="btn btn-sm btn-primary o_stage_add">Add</button>
                <button class="btn btn-sm btn-primary o_stage_add_cancel">Discard</button>
            </div>
        </div>
    </t>

</template>
